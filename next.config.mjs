/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.google.com",
      "encrypted-tbn0.gstatic.com",
      "fakestoreapi.com",
    ],
  },
};

export default nextConfig;
